pipeline {
    agent {
        docker {
            image 'ubuntu:latest'
            //image 'your-docker-image:tag'
            //registryUrl 'https://your-docker-registry'
            //registryCredentialsId 'your-docker-credentials-id'
        }
    }

    stages {
        stage('Stage 1') {
            steps {
                echo 'Running Stage 1...'
                sh 'echo "Sample shell script for Stage 1"'
            }
        }
        stage('Stage 2') {
            steps {
                echo 'Running Stage 2...'
                sh 'echo "Sample shell script for Stage 2"'
            }
        }
        stage('Stage 3') {
            steps {
                echo 'Running Stage 3...'
                sh 'echo "Sample shell script for Stage 3"'
            }
        }
        // Add more stages as needed
    }
}
